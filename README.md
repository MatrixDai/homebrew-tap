# homebrew-tap

Homebrew tap for [snmp-tui](https://github.com/MatrixDai/snmp-tui) — an interactive TUI tool for SNMP MIB exploration and device inspection.

## Install

```bash
brew install MatrixDai/tap/snmp-tui
```

## Usage

```bash
snmp-tui --help
```
